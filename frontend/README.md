# GridironZero Frontend (prototype)

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the backend to be available at `/api/v1` (same host/port in dev you may need a proxy).
